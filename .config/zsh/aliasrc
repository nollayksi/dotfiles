# Use neovim for vim if present.
[ -x "$(command -v nvim)" ] && alias vim="nvim" vimdiff="nvim -d"

# Neovim aliases
alias \
        vi="nvim" \
        vim="nvim"  \
        vimconf="nvim ~/.config/nvim/init.vim"

# Sudo versions also
alias \
        sudo vi="sudo nvim" \
        sudo vim="sudo nvim"

# Colorize commands when possible.
alias \
	ls="ls -hN --color=auto --group-directories-first" \
	grep="grep --color=auto" \
	diff="diff --color=auto" \
	ccat="highlight --out-format=ansi"


# Shortenings
alias p="sudo pacman"
alias ll="ls -la"
alias py="python"

# Netum VPN
alias netumvpn="sh ~/.scripts/netumvpn/netumvpn.sh"

# Disable Away (for teams)
alias disableaway="py ~/.scripts/disableAway/disableAway.py"

#Reload zsh
alias rzsh="source ~/.zshrc"
